【所属分类】01_公众号规划运营
【技能名称】公众号自动化发布
【技能标识】wechat-publisher-auto
【功能描述】微信公众号自动化发布助手（单账号版）。支持增量式配置向导、热点方向选择、多模型文生图（通义万相/DALL-E）、V5 极致排版及草稿箱发布。
【详细概述】
## 触发场景
当用户希望自动化生产并发布微信公众号文章时触发。

## 核心流程
1. **智能配置检查**：启动时自动检测凭证完整性，仅针对缺失项启动增量式交互向导。
2. **热点精准选择**：从预设的 `topics` 列表中让用户手动选择一个创作方向。
3. **AI 配图生成**：根据选题自动生成扁平插画风格封面，支持通义万相（默认）或 DALL-E 3。
4. **素材自动上传**：生成的封面图自动上传至微信素材库，获取 `thumb_media_id`。
5. **V5 极致排版**：使用内置引擎将 Markdown 转换为极致压缩的 HTML，消除段间距。
6. **草稿箱同步**：通过微信 API 将内容同步至草稿箱。

## 配置说明
- **凭证存储**：所有敏感信息（AppID, Secret, API Keys）均通过 AES-256 加密存储在本地 `config.enc`。
- **排版规范**：默认采用 V5 标准，行高 1.75，字间距 1px，两端对齐，HTML 源码极致压缩。
- **模型切换**：在首次配置或补全 Key 时，可自由选择文生图模型。
【安装方式】（把 ZIP 拖进对话框，发一句话就装好）

  第 1 步｜上传
      把本文件夹里的 wechat-publisher-auto.zip 直接拖进 AI 对话框（和发图片一样），
      不用解压、不用找文件夹、不用装任何软件。

  第 2 步｜复制下面这句话，粘贴发送

      「请解压并安装这个技能包：公众号自动化发布。
       装好后告诉我安装结果，以及之后怎么调用它。」

  第 3 步｜收到「安装完成」就能用了
      直接说「使用公众号自动化发布」，或者干脆直接描述你的需求。

  · 一次装多个：把多个 zip 一起拖进对话框，再发
    「请把上面这些技能包全部解压安装」。
  · 不限定哪个 AI：只要你的 AI 支持上传文件、能读写自己的
    技能目录就能用，不绑定平台或品牌。它要是说不会装，补一句：
    「把技能包解压后放进你的技能目录，SKILL.md 在第一层即可」。
  · 装好后不联网也能用；带脚本的技能需要本机有 Python 3.8+。

【使用说明】
· 本技能包由「淘宝店：AI灵匣」整理提供，安装后即可通过自然语言触发。
  · 具体功能以技能包内 SKILL.md 为准，如有问题可重新安装。
